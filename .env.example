# 复制为 .env.local 后按需修改（.env.local 不会提交到 Git）

# 接口基础地址
VITE_API_BASE=https://api.example.com

# 开发阶段无后端时设为 true，使用本地 Mock 数据
VITE_USE_MOCK=true

# 微信小程序 appid（填写后运行 npm run sync:mp-appid 同步到 manifest.json）
VITE_MP_WEIXIN_APPID=
